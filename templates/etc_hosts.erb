#
# /etc/hosts: static lookup table for host names
#

127.0.0.1<%= " #{@local_hostname}.#{@local_domain}" if @local_domain %> <%= @local_hostname %>
::1<%= " #{@local_hostname}.#{@local_domain}" if @local_domain %> <%= @local_hostname %>

127.0.0.1 localhost.localdomain localhost
::1 localhost.localdomain localhost
